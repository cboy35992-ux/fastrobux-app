document.addEventListener("DOMContentLoaded",()=>{const $=id=>document.getElementById(id);$("request").onclick=async()=>{const email=$("email").value.trim();if(!email)return;const button=$("request");button.disabled=true;button.textContent="Sending…";try{const data=await RSRNetwork.api("/api/auth/forgot",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})},{retries:2});$("message").className="message success";$("message").innerHTML=`${data.message}${data.resetUrl?` <a href="${data.resetUrl}">Open development reset link</a>`:""}`;}catch(error){$("message").className="message error";$("message").textContent=error.message}finally{button.disabled=false;button.textContent="Send Reset Email"}}});